import calendarComponent from './components/calendar'

// document.addEventListener('alpine:init', () => {
//     window.Alpine.plugin(calendarComponent)
// })

document.addEventListener('alpine:init', () => {
    Alpine.plugin(calendarComponent)
})
